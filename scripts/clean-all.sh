#!/bin/bash

################################################################################
# Cleans everything. After running this, you will need to run the 
# `bash scripts/install.sh` script again, as the dependencies will be wiped.
################################################################################

rm -rf out-lib
rm -rf out-deps
rm -rf out